function takeANumber(katzDeliLine, name) {
  for (let i = 0; i<katzDeliLine.length; i++)
  return "Welcome, " + name[i] + ". You are number " + katzDeliLine[i] + "in line.";
}

function nowServing(katzDeliLine) {
  
}